% Clear workspace and command window
clear all;
clc;
% Close all figures
close all;

%% Initialise Environment
Environment();

%% Initialise the OmronTM5
% Create an instance of the Omron TM5 robot
feederRobot = OmronTM5();

%% Initialise the AMS Feeder Robot
amsFeederRobot = AMSFeeder();
% Plot the robot at the default position
feederRobot.plotRobot();

% Optionally, move the robot's end-effector to a specific point
targetPoint = [0.5, 0.2, 0.3]; % Example target point
feederRobot.moveToPoint(targetPoint);

%% Create an Instance of the Movement Class
movementController = Movement(feederRobot, amsFeederRobot);
